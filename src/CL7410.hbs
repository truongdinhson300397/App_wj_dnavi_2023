{{#extend "components/layout" title="ダイヤモンド就活ナビ2021：学生のための就職活動・採用情報サイト | メールアドレスの変更・配信停止"}}
    {{#content "header-scripts" mode="append"}}
        <link rel="stylesheet" href="{{path}}css/login.css" type="text/css" media="all"/>
    {{/content}}
    {{#content "main"}}
        <!--  -->
        <div class="contents-box">
            <article class="article-box pddb0">
                <div class="drop-shadow-box">
                    <header class="article-box-header">
                        <h2 class="article-box-header-h2">
                            <span class="article-box-header-h2-jp">メールアドレス変更・配信停止</span>
                            <!-- <br /><span class="article-box-header-h2-eng"></span> -->
                        </h2>
                    </header>
                    <div class="article-box-body">
                        <div class="form-preface">
                            メールアドレス、配信設定を変更しました。
                        </div>
                        <div class="form-btn-box">
                            <a href="{{myPageTop}}" class="btn-default btn-blue w100-50prc">マイページ へ戻る</a>
                        </div>
                    </div>
                </div>
            </article>
        </div>
    {{/content}}
    {{#content "footer-scripts" mode="append"}}
        <script>
            _headerUIHandler(null, null, true);
            var contractTermId = globalInfo("contract_term_id");
            var http = new Http(rootVariables.apiUrl);
            var studentId = globalInfo('id_' + contractTermId);
            var token = globalInfo('jwt_' + contractTermId);


            function scrollTop() {
                $('body, html').animate({scrollTop: 0}, 500);
            }


            $(function () {
                fetchCurrentUser();

                // listening...
                updateEmail();
            });

        </script>
    {{/content}}
{{/extend}}
